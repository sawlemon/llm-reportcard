Delete the accidentally created `new-blog` branch in /Users/sala/Downloads/Personal/llm-reportcard.

Verified: it exists only locally (not on origin), has 0 commits ahead of main, no file diffs vs main, and no worktrees reference it — so deletion is safe and loses nothing.

Command: `git branch -d new-blog` (the safe flag; it will refuse if anything unmerged ever appears). Then confirm with `git branch -a`.
